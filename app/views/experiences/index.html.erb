
  <div class="text-center">
    <h1>
      Build a career profile
    </h1>
  For each resume, you can create a profile, which will show the jobs you wish to display.
  </div>




<hr>


  <div class="text-center">
    <h2>
      Add a job to your profile
    </h2>

    <form action="/insert_experience" method="post" class="row gy-2 gx-3 align-items-center">
      <div class="col-auto">
        <label for="resume_id_box" class="form-label">
          Resume
        </label>

        <select name="query_resume_id" id="resume_id_box" class="form-control">
          <% Resume.all.order(:id).each do |resume| %>
            <option value="<%= resume.id %>">
              <%= resume.name %>
            </option>
          <% end %>
        </select>
      </div>

      <div class="col-auto">
        <label for="job_id_box" class="form-label">
          Job
        </label>

        <select name="query_job_id" id="job_id_box" class="form-control">
          <% Job.all.order(:id).each do |job| %>
            <option value="<%= job.id %>">
              <%= job.title %>
            </option>
          <% end %>
        </select>
      </div>

      

      <div class="col-auto">
        <label for="display_order_box" class="form-label">
          Display order
        </label>

        <input type="text" id="display_order_box" name="query_display_order" class="form-control">
      </div>

      <div class="col-auto">
      <button type="submit" class="btn btn-primary">
        Add to profile
      </button>
      </div>
    </form>
  </div>


<hr>
<%= notice %>

<% Resume.all.order(:id).each do |resume| %>
<h3><%= resume.name %></h3>
  <% resume.experiences.each do |exp| %>
  <div class="row">
  <span class="col-md-6"><%= exp.job.title %></span>
  <a href="/delete_profile/<%= exp.id %>" class="link-danger">Delete</a>
  <span class="text-center"><%= exp.display_order %></span>
  </div>
  <% end %>
<% end %>
